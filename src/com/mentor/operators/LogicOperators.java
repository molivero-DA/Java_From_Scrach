package com.mentor.operators;

public class LogicOperators {

    // Relational operators
    // > = comparable if left is greaten than the right
    // < = comparable if left is less than the right

    // = -> Assigned the value to the left variable, int mynumber = 1234;
    // == -> Compare the values value ( X == Y )
    // != -> Negative comparition between values ( X != Y )

}
