package com.mentor.collections;

import java.util.Collections;
import java.util.List;

public class Lists {

    java.util.List list = Collections.emptyList();
        list.add(1);
        list.add(2);
        list.add(3);
        list.add(4);
        System.out.println(list.size());

    // if() conditionals statement
        if (list.contains(3)) {
        // logic
    }
}
